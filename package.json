{
  "name": "caching-proxy",
  "version": "1.0.0",
  "description": "CLI tool that starts a caching proxy server <https://roadmap.sh/projects/caching-server>.",
  "main": "dist/cli.js",
  "bin": {
    "caching-proxy": "./dist/cli.js"
  },
  "scripts": {
    "start": "ts-node src/cli.ts",
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "proxy",
    "cache",
    "cli",
    "typescript"
  ],
  "author": "conoremcd",
  "license": "ISC",
  "dependencies": {
    "commander": "^14.0.3",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "@types/node-fetch": "^2.6.13",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
